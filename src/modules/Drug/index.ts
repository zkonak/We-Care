import router from "./router.js";
import DrugController from "./controller.js";
import Drug from "./model.js";
const models = { Drug };

/*const controller = new DrugController(models);
const routes = router(controller);

export default routes;*/